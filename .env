# Committed on purpose: this file holds no secrets, only the path to the local
# SQLite file. Keeping it in the repo makes setup a single command.
# Machine-specific overrides belong in .env.local, which is git-ignored.
DATABASE_URL="file:./dev.db"
